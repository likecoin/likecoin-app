import {
  StyleSheet,
  ViewStyle,
} from "react-native"

import { spacing } from "../../theme"

export const StakingRedelegationSigningScreenStyle = StyleSheet.create({
  LearnMoreLink: {
    marginTop: spacing[3],
  } as ViewStyle,
})

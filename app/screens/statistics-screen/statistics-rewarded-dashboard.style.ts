import { StyleSheet, ViewStyle } from "react-native"

import { spacing } from "../../theme"

export const StatisticsRewardedDashboardStyle = StyleSheet.create({
  ChartDimmed: {
    opacity: 0.4,
  } as ViewStyle,
  GetRewardsButton: {
    marginTop: spacing[5],
  } as ViewStyle,
})

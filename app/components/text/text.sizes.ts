/**
 * All the variations of text size within the app.
 */
export const sizes = {
  default: 12,
  medium: 14,
  large: 24,
}

/**
 * A list of size names.
 */
export type TextSize = keyof typeof sizes
